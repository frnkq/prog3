Modificar, Borrar json

funciones:
  listaralumnos
    ->listaralumnos(sourcE)
      ->listaralumnosmysql
        ->AlumnoDAO::GetAlumno
      ->listaralumnojson
        ->alumnodaofiles::getalumno
      ->crearalumnomysql
        crea alumno, llama a guardar

modificar en daofiles (update en dao files)
copy dao to daofiles

use daofiles as much as possible on funciones 

funciones -> crear() hace lo pertinente -> usa dao(files) para get, guardar
funciones -> modificar() hace lo pertinente -> usa dao(files) para get, guardar
funciones -> borrar() hace lo pertinente -> usa dao(files) para get, borrar, guardar array
funciones -> listar() hace lo pertinente -> usa dao(files) para get y formatea

generalizar daos para que sean "object" y no "alumno"

poder hacer un post para "modificarlafoto"
dnied access on not index
clase intermedia "negocio" que verifique
dao->guardar

TODO JSON
Todas las clases FromJson() ToJson()
SaveAlumno -> check if id exists // legajo exists // do some validations
error validation on sql execution

